# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# The LLM judge (voanfw/judge.py) reads these to score gray-zone actions.
OPENAI_API_KEY=
# Any cheap, fast chat model works for the judge. Override as you like.
OPENAI_MODEL=gpt-4o-mini
